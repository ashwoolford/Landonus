<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ash.landonus">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddPoperty"
            android:theme="@style/AppTheme1" />
        <activity
            android:name=".FlatListActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".FlatEditorActivity"
            android:theme="@style/AppTheme1" />
        <activity
            android:name=".FlatDetailsActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".MakingBillsActivity"
            android:theme="@style/AppTheme"/>
        <activity android:name=".CollectBillsActivity"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>